/**
 * Re-exporta desde services/cliente para mantener compatibilidad.
 * La implementación real vive en la capa de Services.
 */
export { syncClienteProfile } from "@/services/cliente/cliente.actions";
